doctype html
html
  head
    meta(charset='utf-8')
  body
    ul#account
      if user
        h1
          |안녕하세요! #{user.displayName}님
        h3
          a(href='/auth/logout') Logout
    ul
      if user.displayName == '학식 관리자'
        li
          a(href='/food/admin') 학식 메뉴 관리
      if user.displayName == '교내전화 관리자'
        li
          a(href='/phone/admin') 교내전화번호 관리
